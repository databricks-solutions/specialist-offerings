[project]
name = "sdp-demo"
version = "0.1.0"
description = "SDP demo: tag-driven expectations from a rules table"
requires-python = ">=3.11"
dependencies = [
    "polars>=1.0",
    "mimesis>=18.0",
    "databricks-sdk>=0.34",
]
